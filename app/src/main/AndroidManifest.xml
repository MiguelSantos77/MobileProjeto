<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoAndroid"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity android:name=".paises.Listar" />

        <activity android:name=".paises.Criar" />

        <activity android:name=".clubes.Listar" />

        <activity android:name=".clubes.Criar" />


        <activity android:name=".jogadores.Listar" />

        <activity android:name=".jogadores.Criar" />

        <activity android:name=".clubes.ListarUser" />
        <activity android:name=".paises.ListarUser" />
        <activity android:name=".jogadores.ListarUser" />

        <activity android:name=".Register" />
        <activity android:name=".Login" />

        <activity android:name=".MenuAdmin" />
        <activity android:name=".Menu" />

        <activity android:name=".jogadores.JogadorDetalhe" />

        <activity android:name=".jogadores.CriarAvaliacao" />
    </application>

</manifest>